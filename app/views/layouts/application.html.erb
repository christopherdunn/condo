<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Condo Managr" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
  </head>

  <body>
    <nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href="/">Condo Managr</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <!-- <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li> -->
  </ul>

  <section class="top-bar-section">

    <!-- Right Nav Section -->
    <ul class="right">
      <%- if user_signed_in? -%>
      <% user = current_user.id %>
      <% hoa = current_user.hoas.first.id %>
      <li class="has-dropdown">
        <a href="#">Votes</a>
        <ul class="dropdown">
          <li><a href="#">Create a Vote</a></li>
          <li><a href="#">Open Votes</a></li>
          <li><a href="#">Past Votes</a></li>
          <!-- <li class="active"><a href="#">Active link in dropdown</a></li> -->
        </ul>
      </li>
      <li class="has-dropdown">
        <a href="#">Projects/Repairs</a>
        <ul class="dropdown">
          <li><%= link_to 'Projects', hoa_projects_path(hoa) %></li>
          <li><%= link_to 'Repairs', hoa_repairs_path(hoa) %></li>
          <li><%= link_to 'Request Repair', new_hoa_repair_path(hoa), method: :get %></li>
        </ul>
      </li>
      <li><%= link_to 'Docs', hoa_condo_documents_path(hoa), method: :get %></li>
      <li class="has-dropdown">
        <a href="#">Budget</a>
        <ul class="dropdown">
          <li><a href="#">Current Budget</a></li>
          <li><a href="#">Past Budgets</a></li>
          <li><a href="#">Performance</a></li>
        </ul>
      </li>
      <li><%= link_to 'Update Account', edit_user_registration_path, method: :get %></li>
                <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
                <%- else -%>
                <li><%= link_to 'Sign In', new_user_session_path %></li>
                <li><%= link_to 'Sign Up', new_user_registration_path %></li>
      <% end %>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">

    </ul>
  </section>
</nav>
    <div id="app">

    <%= yield %>
    <p class="notice"><%= notice %></p>
     <p class="alert"><%= alert %></p>
    <%= javascript_include_tag "application" %>
  </body>
</html>
